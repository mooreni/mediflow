# Code Review Violation Log

One line per entry: short description of the problem and the fix. No duplicates — including entries that teach the same underlying principle, even if the surface details differ.

## Entries

- `visualize.py` `_doc_type_from_id`: missing docstring (purpose, args, return) — added full docstring per Code Standards rule.
- `visualize.py` `_aggregate`: `doc_id` accessed on the row without being in `required_keys`, giving an unhelpful bare `KeyError` — added `doc_id` to the validated keys so the error message states what failed, what the input was, and what was expected (Error Handling rule).
- `llm_judge.py` `EvaluationResult`: dataclass had no docstring — added class-level docstring describing purpose and attributes (Code Standards rule).
- `llm_judge.py` `_is_retryable`: one-liner docstring was missing Args and Returns sections — expanded to full docstring (Code Standards rule).
- `dataset.py`: missing module-level description comment — added top-level docstring per Code Standards rule.
- `dataset.py` `DatasetDoc`: dataclass had no docstring — added class-level docstring with Attributes section per Code Standards rule.
- `dataset.py` `_load_docs`: missing docstring (purpose, args, return) — added full docstring per Code Standards rule.
- `dataset.py` file-reading calls used bare `path.read_text` with no existence check, so failures produced unhelpful OS errors with no context — extracted `_read_doc` helper with a `FileNotFoundError` stating what failed and what was expected (Error Handling rule).
- `dataset.py` `load_eval_docs_by_type` and `load_summary_train_docs`: duplicated path-construction and DatasetDoc-building loops already present in `_load_docs` — unified through the new `_read_doc` helper (Maintainability/no-duplication rule).
- `s5_gemini_flash_dspy_predict.py` `translate`: called `get_translate_client()` inside the translate method, creating an I/O dependency inside business logic — moved client construction to `__init__` and stored as `self._token_client` (Design: keep I/O out of business logic; pass dependencies in).
- `s5_gemini_flash_dspy_predict.py` `translate`: `ValueError` message stated what failed and input doc_id but not what was expected — expanded message to include "expected a non-empty Russian string" (Error Handling: messages must state what failed, what the input was, and what was expected).
- `run_benchmark.py` `_build_scenarios`: bare `KeyError` raised on unknown scenario name with no context — added explicit guard raising `KeyError` stating what the bad input was and what was expected (Error Handling: messages must state what failed, what the input was, and what was expected).
- `splitter.py` `split_document`: called `get_translate_client()` inside the function body, coupling business logic to I/O — added optional `client` parameter defaulting to `None` and resolved the client at the top of the function body so tests and callers can inject a mock (Design: keep I/O out of business logic; pass dependencies in).
- `test_splitter.py` `_make_client_mock` and `_well_formed_payload`: one-liner docstrings missing Args and Returns sections — expanded to full docstrings per Code Standards rule.
- `s3_gemini_pro_dspy_predict_v2.py` `__init__`: `get_translate_client()` was resolved lazily inside `translate_section`, coupling business logic to I/O — moved resolution to `__init__` and stored as `self._client` (Design: keep I/O out of business logic; pass dependencies in).
- `s3_gemini_pro_dspy_predict_v2.py` `__init__`: bare `os.environ["GOOGLE_CLOUD_PROJECT"]` key access produced an unhelpful `KeyError` with no context — replaced with an explicit guard raising `KeyError` stating what failed and what was expected (Error Handling: messages must state what failed, what the input was, and what was expected).
- `llm_judge.py` `score_document`: `except Exception` caught all exceptions without re-raising, violating the specific-exception-types rule — replaced with `except (ValueError, asyncio.TimeoutError, TimeoutError, errors.ClientError)` matching the documented raises from `score_section` (Error Handling: specific exception types only; no bare `except:` unless re-raising).
- `test_evaluation_fixes.py` `make_client_cycled` and `make_perfect_client`: nested helper functions inside test cases had no docstrings — added full docstrings (purpose, args, return) per Code Standards rule.
- `db.py` `load_all_results` and `load_section_results`: `except json.JSONDecodeError` silently swallowed parse errors and substituted an empty list, hiding corrupt data — replaced with `raise ValueError` stating the method name, the result id, the raw input, and what was expected (Error Handling: messages must state what failed, what the input was, and what was expected).
- `runner.py` `load_split_cache`: used `Section(...)` constructor but `Section` was never imported from `src.pipeline.splitter` — added `Section` to the import (Root cause over symptoms; would raise `NameError` at runtime).
- `runner.py` `run_scenario` future-result loop: `except Exception` caught all exceptions without re-raising — narrowed to `except RuntimeError`, which is the only type `_translate_and_evaluate` raises after exhausting retries (Error Handling: specific exception types only; no bare `except:` unless re-raising).
- `s7_flash_translate_pro_correct_v2.py` `FlashTranslateProCorrectV2Scenario`: declared `supports_sections = True` but never overrode `translate_section()`, causing a guaranteed `NotImplementedError` at runtime on every section call — added `translate_section()` that delegates to `_translate_section_flash()` and wraps the result in a `SectionTranslationResult` (Root cause over symptoms; Design: single responsibility).
- `s7_flash_translate_pro_correct_v2.py` `_correct_section_pro`: method was defined but never called — `translate_section()` only delegated to `_translate_section_flash()`, leaving `_correct_section_pro` as dead code — completed `translate_section()` to run the full TRANSLATE→MIDWAY_EVAL→CORRECT pipeline, calling `_correct_section_pro` when midway errors are found (Maintainability: no dead code).
- `llm_judge.py` `_score_async` and `_score_section_async`: called `get_eval_client()` inside the async business-logic functions, coupling I/O to logic — moved client resolution to the public sync entry points (`score`, `score_section`) and passed `client` as an explicit parameter into the async helpers (Design: keep I/O out of business logic; pass dependencies in).
- `s7_flash_translate_pro_correct_v2.py` `translate_section`: four `print()` calls embedded inside the business-logic method mixed I/O with pipeline orchestration — removed all four prints (Design: keep I/O out of business logic).
